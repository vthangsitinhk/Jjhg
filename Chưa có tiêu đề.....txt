--[[
    Banana Hub - Blox Fruits
    Tính năng:
    - Auto Farm Level & Mastery
    - Auto Raid (tự động làm raid)
    - Auto Collect Fruits
    - Teleport đến các đảo
    - ESP (hiển thị trái cây, người chơi)
    - Tự động nâng chỉ số (Stats)
    - Bật/tắt bằng GUI
    Sử dụng Kavo UI Library
]]

-- Kiểm tra và tải Kavo UI Library
if not game:IsLoaded() then game.Loaded:Wait() end

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/UILibrary.lua"))()
local Window = Library.CreateLib("Banana Cat Hub", "DarkTheme")

-- Tab chính
local MainTab = Window:NewTab("Main")
local MainSection = MainTab:NewSection("Auto Farm")

-- Các biến toàn cục
local autoFarmLevel = false
local autoFarmMastery = false
local autoCollectFruit = false
local autoRaid = false
local selectedIsland = "Pirate Starter"

-- Hàm lấy đảo theo tên
function getIslandPosition(name)
    local islands = {
        ["Pirate Starter"] = CFrame.new(-1155, 80, 2980),
        ["Marine Starter"] = CFrame.new(2380, 80, -1080),
        ["Desert"] = CFrame.new(-1360, 90, -2350),
        ["Skylands"] = CFrame.new(-4850, 800, 1250),
        ["Frozen Village"] = CFrame.new(1100, 110, 1350),
        ["Prison"] = CFrame.new(-5250, 100, -850),
        ["Magma Village"] = CFrame.new(-5200, 30, 850),
        ["Underwater City"] = CFrame.new(3850, 20, -2850),
        ["Fountain City"] = CFrame.new(5250, 20, 4450),
        ["Marine Fortress"] = CFrame.new(2800, 10, -3650),
        ["Graveyard"] = CFrame.new(-2750, 50, -4600),
        ["Ice Castle"] = CFrame.new(6000, 400, -1250),
        ["Forgotten Island"] = CFrame.new(-550, 120, -4250),
        ["Usoap's Island"] = CFrame.new(4750, 10, 7150),
        ["Green Zone"] = CFrame.new(-2350, 60, -1050),
        ["Great Tree"] = CFrame.new(2775, 360, -7050),
        ["Hot and Cold"] = CFrame.new(5900, 75, -2850),
        ["Cake Land"] = CFrame.new(-1550, 80, 6950),
        ["Sea of Treats"] = CFrame.new(-2250, 25, 7450)
    }
    return islands[name] or islands["Pirate Starter"]
end

-- Hàm auto farm level
spawn(function()
    while wait(0.5) do
        if autoFarmLevel then
            local player = game.Players.LocalPlayer
            local character = player.Character
            if character and character:FindFirstChild("HumanoidRootPart") then
                local hrp = character.HumanoidRootPart
                local nearestNPC = nil
                local minDist = math.huge
                for _, v in pairs(workspace.Enemies:GetChildren()) do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                        local dist = (hrp.Position - v.HumanoidRootPart.Position).magnitude
                        if dist < minDist then
                            minDist = dist
                            nearestNPC = v
                        end
                    end
                end
                if nearestNPC then
                    hrp.CFrame = nearestNPC.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                    wait(0.1)
                    -- Gửi yêu cầu tấn công
                    local args = {
                        [1] = nearestNPC.HumanoidRootPart.Position,
                        [2] = nearestNPC
                    }
                    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Buso")
                    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Click", args)
                end
            end
        end
    end
end)

-- Auto farm mastery (tương tự)
spawn(function()
    while wait(0.5) do
        if autoFarmMastery then
            -- Tương tự auto farm level nhưng ưu tiên quái có mastery thấp
            -- (code tương tự, có thể mở rộng)
        end
    end
end)

-- Auto collect fruit
spawn(function()
    while wait(0.2) do
        if autoCollectFruit then
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Tool") and v:FindFirstChild("Handle") and string.find(v.Name, "Fruit") then
                    local player = game.Players.LocalPlayer
                    local char = player.Character
                    if char and char:FindFirstChild("HumanoidRootPart") then
                        char.HumanoidRootPart.CFrame = v.Handle.CFrame
                        wait(0.1)
                        fireclickdetector(v.Handle.ClickDetector)
                        wait(0.5)
                    end
                end
            end
        end
    end
end)

-- Auto raid
spawn(function()
    while wait(1) do
        if autoRaid then
            local args = {
                [1] = "CakePrince",
                [2] = true
            }
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Raids", args)
        end
    end
end)

-- Auto stats (nâng chỉ số)
local autoStats = false
spawn(function()
    while wait(1) do
        if autoStats then
            local args = {
                [1] = "Melee",
                [2] = 5
            }
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("AddPoint", args)
        end
    end
end)

-- Nút bật/tắt auto farm
MainSection:NewToggle("Auto Farm Level", "Tự động đánh quái để lên level", function(state)
    autoFarmLevel = state
end)

MainSection:NewToggle("Auto Farm Mastery", "Tự động farm mastery", function(state)
    autoFarmMastery = state
end)

MainSection:NewToggle("Auto Collect Fruit", "Tự động nhặt trái cây rơi", function(state)
    autoCollectFruit = state
end)

MainSection:NewToggle("Auto Raid", "Tự động làm raid (Cake Prince)", function(state)
    autoRaid = state
end)

MainSection:NewToggle("Auto Stats", "Tự động cộng điểm vào Melee", function(state)
    autoStats = state
end)

-- Tab Teleport
local TeleportTab = Window:NewTab("Teleport")
local TeleportSection = TeleportTab:NewSection("Teleport to Island")

local islandDropdown = TeleportSection:NewDropdown("Select Island", "Chọn đảo muốn đến", {"Pirate Starter", "Marine Starter", "Desert", "Skylands", "Frozen Village", "Prison", "Magma Village", "Underwater City", "Fountain City", "Marine Fortress", "Graveyard", "Ice Castle", "Forgotten Island", "Usoap's Island", "Green Zone", "Great Tree", "Hot and Cold", "Cake Land", "Sea of Treats"}, function(selected)
    selectedIsland = selected
end)

TeleportSection:NewButton("Teleport", "Đến đảo đã chọn", function()
    local player = game.Players.LocalPlayer
    local char = player.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        char.HumanoidRootPart.CFrame = getIslandPosition(selectedIsland)
    end
end)

-- Tab ESP
local ESPTab = Window:NewTab("ESP")
local ESPSection = ESPTab:NewSection("ESP Options")

local espFruit = false
local espPlayer = false

-- Hàm vẽ ESP (đơn giản)
local function createESP(part, color)
    local bill = Instance.new("BillboardGui")
    bill.Name = "ESP"
    bill.Adornee = part
    bill.Size = UDim2.new(0, 100, 0, 30)
    bill.AlwaysOnTop = true
    local label = Instance.new("TextLabel", bill)
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = color
    label.TextStrokeTransparency = 0
    label.Text = part.Name
    return bill
end

ESPSection:NewToggle("ESP Fruit", "Hiển thị vị trí trái cây", function(state)
    espFruit = state
    if not espFruit then
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "ESP" then v:Destroy() end
        end
    end
    spawn(function()
        while espFruit do
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Tool") and v:FindFirstChild("Handle") and string.find(v.Name, "Fruit") and not v:FindFirstChild("ESP") then
                    local esp = createESP(v.Handle, Color3.fromRGB(255, 200, 0))
                    esp.Parent = v.Handle
                end
            end
            wait(0.5)
        end
    end)
end)

-- Tab Settings
local SettingsTab = Window:NewTab("Settings")
local SettingsSection = SettingsTab:NewSection("Cài đặt")

SettingsSection:NewButton("Refresh GUI", "Làm mới giao diện (nếu lỗi)", function()
    Library:Close()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/UILibrary.lua"))()
end)

SettingsSection:NewButton("Destroy GUI", "Xóa giao diện", function()
    Library:Close()
    for _, v in pairs(game.CoreGui:GetChildren()) do
        if v.Name == "KavoUI" then v:Destroy() end
    end
end)

-- Thông báo khởi tạo
Library:Notification("Banana Cat Hub", "Đã tải thành công!", "OK", 3)